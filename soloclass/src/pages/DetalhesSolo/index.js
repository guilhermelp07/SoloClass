import { Text, View } from "react-native"

import Styles from "../../styles/Styles";

export default function DetalhesSolo(){
    return(
        <View style={Styles.container}>
            <Text>Detalhes do Solo</Text>
        </View>
    );
}